---
title: "Farm the Basis"
sidebarTitle: "Farm the Basis"
description: "Evaluate the funding spread, collateral yield, capital efficiency, and risks of a hedged Null Payer position."
---

Basis farming combines a short position on Null through Payer with a long position elsewhere. The external long offsets much of Payer's tracked-price exposure. What remains is a carry trade that earns only when Null funding and collateral yield exceed external funding, execution costs, and basis losses.

## The return sources

A hedged Payer position has three projected carry legs:

1. **Null funding** paid to or by Payer.
2. **Collateral yield** earned by the value held in Payer when the collateral is yield-bearing.
3. **External funding** received or paid by the long hedge.

Trading fees, slippage, gas, liquidation losses, and oracle-to-venue basis changes sit outside this carry estimate.

## Calculate expected carry

Let:

- `p` be your Payer position value;
- `E = R / P` allocate Receiver exposure across Payer shares, using current Receiver assets `R` and Payer assets `P`;
- `f` be Null's signed market funding rate produced by the market's funding-rate model, where positive means Receiver pays Payer;
- `f_ext` be the external funding rate, where positive means the external long pays; and
- `y` be the collateral yield rate.

The hedge notional is:

```text
H = pE
```

Projected annual carry is:

```text
Null funding       =  H * f
Collateral yield   =  p * y
External funding   = -H * f_ext

Net carry          =  p * [y + E * (f - f_ext)]
```

Both funding rates apply to the same exposure notional `H`. Null does not multiply `f` into a separate Payer funding rate: `E` determines how much Receiver notional belongs to the Payer position.

Compare raw Null funding `f` with external funding `f_ext` directly, then apply the spread to `H`.

<Info>
The sign of the opportunity comes from `f - f_ext`. The allocated exposure notional `H = pE` determines the size of that funding spread, while collateral yield `y` adds a separate carry source.
</Info>

## Worked example

Assume:

```text
Payer position p        = $100
Exposure allocation E   = 5
Null funding f          = 1.5%
External long funding   = 1%
Collateral yield y      = 5%
```

The Payer position represents `$500` of Receiver exposure, so the external hedge notional is also `$500`. Projected annual carry is:

```text
Null funding       = $500 * 1.5%     =  $7.50
Collateral yield   = $100 * 5%       =  $5.00
External funding   = $500 * 1%       = -$5.00
Net carry          =                      $7.50
```

If external funding rises to `3%`, Null funding remains 1.5% of the same exposure notional, but projected annual net carry becomes:

```text
$5 + $500 * (1.5% - 3%) = -$2.50
```

A larger allocated exposure notional increases a negative funding spread just as readily as a positive one. Capital posted as external margin also affects the realized return on all capital deployed.

## Where Null funding comes from

The standard imbalance funding-rate model uses:

```text
f = mu * (cR - P) / (cR + P)
```

For the standard imbalance funding-rate model:

- `c` is the immutable target Payer coverage ratio where funding crosses zero;
- `mu` is the immutable model sensitivity that bounds and scales the curve;
- `R` is Receiver assets; and
- `P` is Payer assets.

Each market references a funding-rate model deployment. `c` and `mu` come from that model's onchain parameters, while `R` and `P` come from current pool state. The model computes `f` from those values. Do not assume every interface exposes each input separately; verify the market's model and onchain state when evaluating the trade.

When `P < cR`, Payer is scarce and `f` is positive: Receiver pays Payer. When `P > cR`, funding reverses and Payer pays Receiver. At `P = cR`, Null funding is zero.

When Payer backing is scarce, each Payer share represents more Receiver exposure. Evaluate the funding rate against that exposure notional.

## Screen an opportunity

Before opening the trade, estimate:

```text
expected net carry
- entry and exit fees
- expected rebalancing fees and slippage
- gas
- margin opportunity cost
- conservative basis-loss allowance
```

Review at least:

- current and historical Null funding;
- current and predicted external funding;
- Receiver/Payer coverage and how quickly it is changing;
- collateral yield quality and withdrawal behavior;
- oracle and external-market trading hours;
- available Payer liquidity;
- venue depth, lot size, minimum notional, and leverage limits; and
- the amount of equity required to keep the external long away from liquidation.

Do not open solely because one funding snapshot looks attractive. Both funding rates can change sign.

## Understand basis PnL

The Null leg settles against its oracle, while the external long is marked and filled at the venue price. Those prices can diverge.

The reference hedger tracks:

```text
Null PnL = current Payer value - opening value - deposits and withdrawals

Hedge PnL = change in unrealized PnL
          + realized PnL
          + external funding
          - venue fees

Net PnL = Null PnL + Hedge PnL
```

After separating carry and execution costs, the remaining residual is basis PnL. It includes oracle-versus-venue price drift and entry or rebalance slippage. A position can have attractive projected carry and still lose money through basis or execution.

## Manage the hedge

Follow the sizing and no-trade-band process in [Market Making](/private-beta/market-make/overview). Recompute from fresh pool state instead of trading every price move. Stop automated resizing when Null state is stale or an exceptional settlement makes the ordinary hedge assumptions unreliable.

## Key risks

### Funding reversal

Null funding can fall or turn negative, and external funding can rise. The spread may disappear before entry costs are recovered.

### Basis risk

Null's oracle and the external venue can mark the asset differently, particularly during volatility, closures, or stale data.

### External liquidation

Null has no liquidation engine, but the hedge venue does. Insufficient external equity can liquidate the long even while the combined trade is economically hedged.

### Flow and liquidity risk

Large mints or burns change `R/P` and the hedge target. Thin Payer liquidity can also affect entry and exit.

### Execution risk

Fees, slippage, partial fills, minimum order sizes, downtime, and API failures can leave the two legs mismatched.

### Protocol and collateral risk

Smart-contract, oracle, collateral, vault, and settlement risks remain on the Null leg.

<Warning>
“Delta neutral” describes the intended price exposure at a point in time. It does not mean risk neutral, capital protected, or guaranteed profitable.
</Warning>

Start with [Market Making](/private-beta/market-make/overview) for the hedge lifecycle and sizing formula.
