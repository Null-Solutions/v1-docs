---
title: "Smart Contracts"
sidebarTitle: "Smart Contracts"
description: "A high-level guide to the contracts that create Null markets, hold collateral, settle returns, and issue positions."
---

Null is a small set of Ethereum smart contracts that turn collateral, an oracle, and a funding model into a synthetic market. The contracts hold the market's collateral, calculate how value moves between Receiver and Payer, and issue tokenized shares representing each position.

There is no offchain risk engine deciding whether a position remains open. The market follows rules fixed in its contracts and settles using public onchain state.

## The contracts at a glance

### Factory

`NullV1Factory` creates and registers markets. Each market is uniquely defined by:

```text
(collateral, oracle, funding-rate model)
```

Anyone can create a market from a funding-rate model that the Factory has enabled. The Factory prevents duplicate markets with the same combination and acts as the canonical directory for finding deployed markets.

### Market

`NullV1Market` is the accounting core. It:

- holds collateral for both sides;
- records how much belongs to Receiver and Payer;
- reads the oracle and funding-rate model during settlement;
- calculates price returns, funding, and protocol fees; and
- moves collateral between the two pool balances.

The Market holds the collateral directly. The pool contracts do not maintain separate collateral reserves.

### Receiver and Payer pools

Every Market has two `NullV1Pool` contracts:

- **Receiver Pool** — issues shares representing synthetic exposure to the tracked price.
- **Payer Pool** — issues shares representing the collateral backing Receiver and the opposite side of its return.

Both pools follow the ERC-4626 tokenized-vault standard. Depositing collateral mints transferable pool shares; redeeming those shares returns the holder's proportional claim on that pool's current collateral.

### Oracle

The oracle reports the tracked asset's price. Null markets can use different oracle adapters, provided they return data in the format expected by the protocol.

The Market compares the new accepted price with its previous price to calculate the Receiver return. The tracked asset itself never needs to enter the protocol.

### Funding-rate model

The funding-rate model, or FRM, determines the signed rate paid between Receiver and Payer. The standard model responds to the amount of collateral on each side:

- when Payer backing is scarce, Receiver can pay Payer;
- when Payer backing is abundant, Payer can pay Receiver; and
- at the model's target coverage, funding is zero.

The Market's FRM is selected when the Market is created and cannot be replaced for that existing Market.

### Routers and read helpers

The supporting contracts make the core easier and safer to use:

- `NullV1Router` and `NullV1PayableRouter` combine market actions with deadlines, minimum-output checks, and optional oracle update data.
- `NullV1Lens` previews a Market after applying current settlement inputs.
- `NullV1Multicall` batches read-only calls and simulations.

Routers do not own user positions. Pool shares are minted directly to the chosen wallet.

## How a deposit works

<Steps>
  <Step title="The user chooses a side">
    The user selects Receiver or Payer and specifies how much market collateral to deposit.
  </Step>
  <Step title="The Market settles">
    Before calculating new shares, the Pool asks the Market to bring its accounting up to date using the latest accepted price, funding, fees, and collateral balance.
  </Step>
  <Step title="Collateral enters the Market">
    The collateral token moves from the user to the Market contract. Any entry fee is separated from the assets credited to the selected Pool.
  </Step>
  <Step title="The Pool mints shares">
    The Pool converts the credited collateral into ERC-4626 shares and sends them to the recipient.
  </Step>
</Steps>

## How settlement works

Settlement is interaction-driven. The contracts do not require a keeper to update every block. A deposit, mint, withdrawal, redemption, or certain owner operations trigger settlement before continuing.

At a high level, the Market:

1. reconciles its recorded accounting with the collateral it actually holds;
2. accrues the configured collateral fee;
3. reads the oracle price and signed funding rate;
4. calculates the price transfer and funding transfer;
5. nets those values into one transfer between Receiver and Payer;
6. limits the transfer so the paying Pool cannot spend unavailable collateral; and
7. stores the updated Pool balances, fees, price, and timestamp.

If an oracle move exceeds the time-adjusted circuit-breaker limit, the Market skips that price transfer and adopts the new price as its next baseline. Funding can still settle.

<Info>
Settlement changes the amount of collateral represented by each Pool. It does not mint or burn existing users' shares, so gains and losses appear through the value of each share.
</Info>

## How a redemption works

The exit process mirrors entry:

1. the Market settles first;
2. the Pool calculates the collateral represented by the submitted shares;
3. the shares are burned;
4. any exit fee is reserved for the protocol; and
5. the Market transfers the remaining collateral to the recipient.

Because settlement happens before execution, the final result can differ from a preview based only on the last stored state. Routers let users set minimum outputs so the transaction reverts when that difference is unacceptable.

## What is fixed and what is controlled

For an existing Market, its collateral token, oracle, funding-rate model, Receiver Pool, and Payer Pool are fixed. An administrator cannot silently swap those components.

The protocol owner can:

- enable additional funding-rate models for future markets;
- set entry, exit, and collateral fees within the contract's hard caps; and
- collect fees already reserved by a Market.

These controls do not let the owner seize Pool collateral, close user positions, liquidate an account, or replace an existing Market's oracle or funding model.

## What the contracts do not contain

Null's core contracts do not include:

- user margin accounts;
- leverage or borrowing;
- a liquidation engine;
- an order book;
- custody of the tracked asset;
- an offchain or discretionary risk engine; or
- automated market-maker hedging.

External interfaces, indexers, and hedgers can build around the contracts, but they do not change the accounting rules enforced by a Market.

For exact interfaces and integration details, continue to the [Developer overview](/developers/overview) or explore the [Factory](/contracts/factory), [Market](/contracts/market), and [Pool](/contracts/pool) references.
