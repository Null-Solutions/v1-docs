---
title: "Smart contracts"
sidebarTitle: "Smart Contracts"
description: "A high-level map of Null's contract structure: factory, market, pools, and helpers."
---

Null's private beta runs on Ethereum mainnet smart contracts. This page is a structural overview. For ABIs, function surfaces, and integration detail, use the [Developers](/developers/overview) docs.

## Structure

```text
Factory ──creates──▶ Market
                       ├── Receiver Pool (ERC-4626)
                       └── Payer Pool (ERC-4626)

Router ──user──▶ pools (+ optional oracle update)
Lens / Multicall ──read──▶ market, pools, and previews
```

A market is uniquely identified by an immutable tuple:

```text
(collateral, oracle, funding-rate model)
```

Different dependencies create a different market. Those addresses cannot be swapped on an existing market.

## Core contracts

### Factory

`NullV1Factory` creates and registers markets. Creation is permissionless for enabled funding-rate models. Use the canonical factory on the [addresses page](/resources/addresses) when verifying a market.

### Market

`NullV1Market` holds the collateral, tracks how much belongs to each pool, and settles price PnL, funding, and fees. Settlement runs when someone interacts with a pool. There is no separate public poke each block.

### Receiver and Payer pools

Each market has two `NullV1Pool` contracts. Both issue transferable ERC-4626 shares. The pools track ownership; the market holds the collateral.

## Supporting contracts

| Contract | Role |
| --- | --- |
| **Oracle** | Reports the tracked price used at settlement. The tracked asset never enters Null. |
| **Funding-rate model (FRM)** | Returns the signed funding rate between Receiver and Payer. Fixed at market creation. |
| **Router** | Combines deposits, redemptions, deadlines, slippage checks, and optional oracle updates. |
| **Lens / Multicall** | Settlement-aware previews and batched reads. |

## What stays fixed vs owner-controlled

**Fixed per market:** collateral, oracle, FRM, Receiver pool, and Payer pool.

**Factory owner can:** enable FRMs for future markets, set fees within contract caps, and collect reserved fees. The owner cannot replace an existing market's dependencies or withdraw pool-allocated collateral as admin funds.

## Where to go next

- [Factory](/contracts/factory), [Market](/contracts/market), and [Pool](/contracts/pool) interfaces
- [Ethereum mainnet addresses](/resources/addresses)
- [Risks and parameters](/learn/risks-and-parameters)

<Warning>
Onchain structure lets you inspect the rules and state. It does not guarantee correct code, a correct oracle, safe collateral, or a profitable trade.
</Warning>
